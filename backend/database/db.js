// This is all we need to do to declare a database:
// projeggdb is the database name. We put it in an export method
// so it becomes available elsewhere.

module.exports = {
  db: "mongodb://localhost:27017/projeggdb",
};
